# 문제은행 편집 안내

문제는 `data/architecture.json`, `security.json`, `business.json`, `management.json`에서 관리합니다. 화면·출제·읽기 모드는 모두 같은 JSON을 사용합니다. 문제 문장을 JavaScript에 넣지 않습니다. 공식 기출을 복제하지 말고 근거가 있는 자체 연습문제를 작성하세요.

## 문항 형식

```json
{
  "id": "arch-101",
  "revision": 1,
  "area": "architecture",
  "topic": "학습 주제",
  "prompt": "정답이 하나인 질문",
  "options": ["보기 A", "보기 B", "보기 C", "보기 D"],
  "answer": 0,
  "explanation": "정답인 이유와 다른 보기와의 차이",
  "source": {"url": "https://www.topcit.or.kr/board/study.do", "section": "교재 이름, 절 또는 쪽"}
}
```

`answer`는 원본 `options`의 0부터 시작하는 위치입니다. 문제풀이 화면은 보기 순서를 섞지만 정답 판정은 원본 위치로 합니다. 전체보기·개념모드는 JSON 순서를 유지합니다.

## 추가·수정·삭제와 학생 진도

- **추가:** 한 번도 사용하지 않은 고유 `id`를 발급하고 `revision: 1`로 시작합니다. 새 문제는 미학습으로 합류하며, 이전에 모든 문제를 완료한 학생도 새 문제를 풀 수 있습니다. 총 문항 수는 화면에서 자동 계산합니다.
- **의미 변경:** 질문·보기·정답의 의미나 보기 순서를 바꾸면 `revision`을 1 올립니다. 해당 문항만 진도와 현재 출제가 초기화되고 다른 문제의 진도는 유지됩니다. 정답 자체를 고치는 경우도 반드시 올립니다.
- **문구 정리:** 의미를 바꾸지 않는 오탈자·해설 표현·출처 보완은 `revision`을 유지할 수 있습니다. 정답 판단에 영향을 주는 수정이면 개정 번호를 올리세요.
- **삭제:** JSON에서 문항을 제거합니다. 현재 출제와 진도 집계에서 제외됩니다. 과거 응답 로그는 당시 ID와 개정 번호를 가진 기록으로 남습니다.
- **ID 재사용 금지:** 삭제한 ID를 새 문제에 재사용하지 않습니다. Git 이력과 `data/id-registry.json`을 확인하세요. 등록부의 과거 ID는 지우지 않습니다. 의미가 다른 새 문항에는 새 ID를 사용합니다.

문항을 추가·개정·삭제한 뒤 `npm run registry`로 `data/id-registry.json`을 갱신합니다. 과거 삭제 ID의 재사용과 개정 번호 감소는 거부됩니다. 초기 제공량은 영역별 100문항이며, 향후 추가·삭제에 맞춰 `tests/content.test.mjs`의 기대 문항 수를 조정하세요. 문항의 개정 번호를 과거보다 낮추지 않습니다.

## 검토와 게시

1. 단 하나의 보기가 정답인지, 조건 누락이나 복수 해석이 없는지 확인합니다. 오답에도 실제 개념 차이를 반영하고 해설로 설명합니다.
2. 교재 절·쪽과 출처를 확인합니다. 구판의 법령 수치나 폐기된 보안 권고를 현재 사실로 옮기지 않습니다. 교재 PDF는 저장소에 재배포하지 않습니다.
3. `npm test`로 데이터 형식, 고유ID, 학습 로직, 진도 가져오기를 검증합니다. 질문 의미와 정답 정확성은 자동 형식 검사로 보장되지 않으므로 별도 검토합니다.
4. 로컬 HTTP 서버로 화면을 확인한 뒤 변경 파일을 커밋하고 일반 push합니다. GitHub Pages 배포가 끝난 뒤 공개 화면을 새로고침하여 확인합니다.

## 구현 구조

- `engine.mjs`: 문제별 연속 정답·현재 시도·개정 반영
- `storage.mjs`: 저장 어댑터. 현재 localStorage와 브라우저 Web Locks 사용
- `snapshot.mjs`: 버전 있는 gzip/base64url 진도 스냅샷. 본문·개인정보·전체 응답 로그 제외
- `app.mjs` / `styles.css`: 문제풀이·전체보기·개념모드 공통 화면

브라우저 데이터 삭제 시 localStorage 기록은 지워집니다. 응답기록 내려받기는 기록 보관용이며, JSON 파일 가져오기 기능은 제공하지 않습니다. 기기 이동 링크는 서버 동기화가 아닌 진도 교체이고, 같은 링크를 반복해 열어도 연속 정답을 합산하지 않습니다.
