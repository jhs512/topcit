{
  "name": "topcit-practice",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python -m http.server 4173",
    "test:browser": "node tests/browser.mjs",
    "registry": "node scripts/registry.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
